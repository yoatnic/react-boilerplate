//@flow
import React from "react";

const Header = () => {
  const style = {
    width: "100%",
    height: "40px",
    backgroundColor: "#DDDDDD"
  };
  return (
    <header style={style}>
      <h1>header</h1>
    </header>
  );
};

export default Header;
